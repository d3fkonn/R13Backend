   
    <% @addresses.each do |address| %>
    
    {
       street_address: <%= address.street_address %>,
        city: <%= address.city %>,
          postal_code: <%= address.postal_code %>
    }
    <% end %>
